
#include "Metodi.h"

Metodi::Metodi(){

}

void Metodi::stampaElenco(vector<Persona> p){
	for(vector<Persona>::iterator i = p.begin(); i != p.end(); ++i){
		cout<<"Nome: "<<i->nome<<endl;
		cout<<"Cognome: "<<i->cognome<<endl;
		cout<<"Anno di nascita: "<<i->annonascita<<endl;
		cout<<"Codifce fiscale: "<<i->cf<<endl<<endl;
	}
}

shared_ptr<Persona> inserisciPersona(int year, string cod, string name, string surname){
	shared_ptr<Persona> p (new Persona(year,cod,name,surname));
	return p;
}

Metodi::~Metodi() {
}
